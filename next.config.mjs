/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Commented out to enable standard server mode
    // images: {
    //     unoptimized: true,
    // },
};

export default nextConfig;
